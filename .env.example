# =====================================================================
# LOT Bot - Plantilla de variables de entorno
# ---------------------------------------------------------------------
# Copia este fichero a `.env` y rellena los valores reales.
# `.env` esta en .gitignore: NUNCA se sube al repositorio.
# =====================================================================

# ---------------------------------------------------------------------
# MODO DE FUNCIONAMIENTO
# ---------------------------------------------------------------------
# true  -> usa MockWallapopService (datos simulados, no toca Wallapop)
# false -> usa la integracion real (requiere credenciales validas)
LOT_BOT_DEMO_MODE=true

# Entorno: development | production
LOT_BOT_ENV=development

# Nivel de log: DEBUG | INFO | WARNING | ERROR
LOT_BOT_LOG_LEVEL=INFO

# Idioma de la interfaz: es | en
LOT_BOT_LANGUAGE=es

# ---------------------------------------------------------------------
# CREDENCIALES DE WALLAPOP (Wallapop Connect / integracion autorizada)
# ---------------------------------------------------------------------
# Estos valores los proporciona Wallapop al autorizar la integracion.
# NO inventar valores: sin credenciales reales el bot permanece en DEMO.
WALLAPOP_CLIENT_ID=
WALLAPOP_CLIENT_SECRET=
WALLAPOP_REDIRECT_URI=http://127.0.0.1:8723/callback

# Fichero YAML que describe los endpoints oficiales concedidos a esta
# integracion. Se rellena copiando `config/endpoint_map.example.yaml`
# y completandolo EXCLUSIVAMENTE con la documentacion oficial recibida.
WALLAPOP_ENDPOINT_MAP=

# Ambito (scopes) concedidos, separados por espacios. Tal y como aparezcan
# en la documentacion oficial de la integracion.
WALLAPOP_SCOPES=

# ---------------------------------------------------------------------
# PROVEEDOR DE IA
# ---------------------------------------------------------------------
# provider: anthropic | rules
#   anthropic -> lenguaje natural completo (requiere API key)
#   rules     -> interprete local de ordenes, sin conexion a IA externa
LOT_BOT_AI_PROVIDER=rules
ANTHROPIC_API_KEY=
LOT_BOT_AI_MODEL=claude-sonnet-5
LOT_BOT_AI_MAX_TOKENS=4096

# ---------------------------------------------------------------------
# CIFRADO LOCAL
# ---------------------------------------------------------------------
# Clave maestra para cifrar los tokens en la base de datos local.
# Si se deja vacia, LOT Bot la genera y la guarda en el almacen de
# credenciales del sistema operativo (recomendado).
LOT_BOT_MASTER_KEY=

# ---------------------------------------------------------------------
# NEGOCIO (valores por defecto de las plantillas)
# ---------------------------------------------------------------------
LOT_BOT_BUSINESS_WHATSAPP=
LOT_BOT_BUSINESS_NAME=
